# ─── Interview Copilot – Python Dependencies ───────────────────────────────
#
# Install all dependencies with:
#   pip install -r requirements.txt
#
# On Windows, PyAudio may require a pre-built wheel. If it fails, try:
#   pip install pipwin && pipwin install pyaudio
# ────────────────────────────────────────────────────────────────────────────


# Audio capture – PyAudio with WASAPI loopback support on Windows
PyAudioWPatch>=0.2.12.7

# OpenAI SDK – Used for OpenRouter compatibility
openai>=1.0.0

# Speech Recognition for subtitles
SpeechRecognition>=3.10.0

# Translation for subtitles
deep-translator>=1.11.0

# Pillow for multimodal screen capture (Vision AI)
Pillow>=10.2.0

# GUI
PySide6>=6.6.0

# Global hotkeys for overlay
keyboard>=0.13.5